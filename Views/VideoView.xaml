<UserControl x:Class="mouse_tracking_web_app.Views.VideoView"
         xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
         xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
         xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
         xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
         mc:Ignorable="d"
         d:DesignHeight="300" d:DesignWidth="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Label Content="{Binding Title}" />
        <Label Grid.Column="1">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="{Binding ElementName=txtLimitedInput, Path=Text.Length}" />
                <TextBlock Text="/" />
                <TextBlock Text="{Binding MaxLength}" />
            </StackPanel>
        </Label>
        <TextBox MaxLength="{Binding MaxLength}" Grid.Row="0" Grid.ColumnSpan="2" Name="txtLimitedInput"
                 ScrollViewer.VerticalScrollBarVisibility="Auto" TextWrapping="Wrap" />
        <GroupBox Header="Upload and Process Video" Margin="10" Padding="10" Grid.ColumnSpan="2" Grid.Row="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="2.5*" />
                </Grid.ColumnDefinitions>
                <Button Margin="10,10" Grid.Row="0" Grid.Column="0" x:Name="BtnUploadsVideo" Background="#FFD6EBF2" Click="UploadVideoButtonClicked" Content="Upload" />
                <TextBox Margin="0,0" Grid.Row="0" Grid.Column="1" Width="0" x:Name="videoName" Text="{Binding VM_VideoName, UpdateSourceTrigger=PropertyChanged}" IsReadOnly="True" />
                <TextBox Margin="10,10" Grid.Row="0" Grid.Column="1" x:Name="videoName1"  IsReadOnly="True" />
                <Button Margin="0,10" Width="80" Grid.Row="1" Grid.ColumnSpan="2" x:Name="BtnProcessVideo" Background="#FFCBEAD7" Click="ProcessVideoButtonClicked" Content="Process" />
            </Grid>
        </GroupBox>
        <!--<Button Content="process video" Background="#FFCBEAD7" Grid.Row="2" Grid.ColumnSpan="2" Click="ProcessVideoButtonClicked" />-->
    </Grid>
</UserControl>

<!--<UserControl x:Class="mouse_tracking_web_app.Views.VideoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:mouse_tracking_web_app.Views"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <Button Content="run python" HorizontalAlignment="Left" Height="255" Margin="224,117,0,0" VerticalAlignment="Top" Width="440" />
    </Grid>
</UserControl>-->